TablePy - это простая, но мощная библиотека для работы с табличными данными на Python. Она предоставляет интуитивно понятный API для создания, обработки, фильтрации и сохранения таблиц различных форматов.

🚀 Возможности
📊 Создание таблиц - Простое создание таблиц с поддержкой различных типов данных

🔧 Типизация данных - Автоматическая конвертация и валидация типов (int, float, bool, str)

➕ Арифметические операции - Сложение, вычитание, умножение, деление столбцов

🔍 Фильтрация и сравнение - Операции сравнения и фильтрации данных по условиям

💾 Множественные форматы - Сохранение и загрузка в CSV, Pickle, текстовом формате

🛡️ Обработка ошибок - Подробные исключения и валидация данных

📈 Визуализация - Красивый вывод таблиц в консоль

📦 Установка
bash
# Клонируйте репозиторий
git clone https://github.com/Framyrite/tablepy.git
cd tablepy

# Или просто скачайте файлы:
# table.py, io_csv.py, io_pickle.py, io_text.py
⚡ Быстрый старт
Создание простой таблицы
python
from table import Table

# Создаем таблицу сотрудников
columns = ['Имя', 'Возраст', 'Зарплата', 'Активен']
rows = [
    ['Анна', '25', '50000', 'Да'],
    ['Борис', '30', '75000', 'Да'],
    ['Виктор', '22', '45000', 'Нет']
]

table = Table(columns=columns, rows=rows)
table.print_table()
Вывод:

text
Имя    | Возраст | Зарплата | Активен
-------|---------|----------|---------
Анна   | 25      | 50000    | Да
Борис  | 30      | 75000    | Да
Виктор | 22      | 45000    | Нет
Работа с типами данных
python
# Устанавливаем типы данных
table.set_column_types({
    'Возраст': 'int',
    'Зарплата': 'float', 
    'Активен': 'bool'
}, by_number=False)

# Получаем типизированные значения
ages = table.get_values('Возраст')  # [25, 30, 22]
salaries = table.get_values('Зарплата')  # [50000.0, 75000.0, 45000.0]
Арифметические операции
python
# Добавляем 5000 к зарплате
table.add('Зарплата', 5000, result_column='Зарплата_бонус')

# Увеличиваем зарплату на 10%
table.mul('Зарплата', 1.1, result_column='Зарплата_увеличена')
Фильтрация данных
python
# Находим сотрудников с зарплатой > 60000
high_salary_mask = table.gr('Зарплата', 60000)
rich_employees = table.filter_rows(high_salary_mask)

# Молодые сотрудники (< 30 лет)
young_mask = table.ls('Возраст', 30)
young_employees = table.filter_rows(young_mask)
Сохранение и загрузка
python
from io_csv import save_table as save_csv, load_table as load_csv
from io_pickle import save_table as save_pickle, load_table as load_pickle
from io_text import save_table as save_text

# Сохраняем в разных форматах
save_csv(table, 'сотрудники.csv')
save_pickle(table, 'сотрудники.pkl') 
save_text(table, 'сотрудники.txt')

# Загружаем обратно
loaded_table = load_csv('сотрудники.csv')
📚 Документация
Основные классы
Table
Основной класс для работы с таблицами.

python
Table(columns, rows, types=None)
Параметры:

columns - список названий столбцов

rows - список строк данных

types - словарь типов столбцов (опционально)

Основные методы
Работа с данными
get_values(column) - получить значения столбца

set_values(values, column) - установить значения столбца

get_column_types(by_number=True) - получить типы столбцов

set_column_types(types_dict, by_number=True) - установить типы столбцов

Операции со строками
get_rows_by_number(start, stop=None) - получить строки по номерам

get_rows_by_index(*vals) - получить строки по значениям первого столбца

filter_rows(bool_list) - отфильтровать строки по булеву массиву

Арифметические операции
add(col_a, col_b_or_scalar, result_column=None) - сложение

sub(col_a, col_b_or_scalar, result_column=None) - вычитание

mul(col_a, col_b_or_scalar, result_column=None) - умножение

div(col_a, col_b_or_scalar, result_column=None) - деление

Операции сравнения
eq(col_a, col_b_or_scalar) - равно

gr(col_a, col_b_or_scalar) - больше

ls(col_a, col_b_or_scalar) - меньше

ge(col_a, col_b_or_scalar) - больше или равно

le(col_a, col_b_or_scalar) - меньше или равно

ne(col_a, col_b_or_scalar) - не равно

Ввод/вывод
print_table() - вывод таблицы в консоль

as_dict() - представление таблицы в виде словаря

from_dict(data) - создание таблицы из словаря

Модули ввода/вывода
io_csv.py
load_table(path, delimiter=',', has_header=True, encoding='utf-8') - загрузка из CSV

save_table(table, path, delimiter=',', has_header=True, encoding='utf-8') - сохранение в CSV

io_pickle.py
load_table(path) - загрузка из Pickle

save_table(table, path) - сохранение в Pickle

io_text.py
save_table(table, path, encoding='utf-8') - сохранение в текстовом формате

🎯 Примеры использования
Анализ продаж
python
from table import Table

# Данные о продажах
sales = Table(
    columns=['Товар', 'Цена', 'Количество', 'Скидка'],
    rows=[
        ['Ноутбук', '999.99', '5', '0.1'],
        ['Мышь', '25.50', '20', '0.05'],
        ['Клавиатура', '75.00', '15', '0.0']
    ]
)

# Устанавливаем типы
sales.set_column_types({
    'Цена': 'float', 
    'Количество': 'int', 
    'Скидка': 'float'
})

# Вычисляем общую стоимость
sales.mul('Цена', 'Количество', result_column='Общая_стоимость')
sales.mul('Общая_стоимость', 'Скидка', result_column='Сумма_скидки')
sales.sub('Общая_стоимость', 'Сумма_скидки', result_column='Итог')

sales.print_table()
Обработка научных данных
python
# Данные экспериментов
experiments = Table(
    columns=['Эксперимент', 'Температура', 'Давление', 'Результат'],
    rows=[
        ['Эксп1', '25.5', '101.3', '0.845'],
        ['Эксп2', '30.0', '102.1', '0.923'],
        ['Эксп3', '15.0', '99.8', '0.234']
    ]
)

experiments.set_column_types({
    'Температура': 'float',
    'Давление': 'float', 
    'Результат': 'float'
})

# Нормализация результатов
experiments.mul('Результат', 100, result_column='Результат_проценты')

# Успешные эксперименты (> 70%)
successful = experiments.filter_rows(
    experiments.gr('Результат_проценты', 70)
)
🛠️ Разработка
Структура проекта
text
tablepy/
├── table.py          # Основной класс Table
├── io_csv.py         # CSV импорт/экспорт
├── io_pickle.py      # Pickle импорт/экспорт  
├── io_text.py        # Текстовый экспорт
├── demo.py           # Примеры использования
└── README.md         # Документация
Зависимости
Python 3.7+

Стандартная библиотека Python (csv, pickle)

🤝 Участие в разработке
Мы приветствуем вклад в развитие проекта!

Форкните репозиторий

Создайте ветку для фичи (git checkout -b feature/amazing-feature)

Закоммитьте изменения (git commit -m 'Add amazing feature')

Запушьте в ветку (git push origin feature/amazing-feature)

Откройте Pull Request

👨‍💻 Авторы
Framyrite - Разработчик
